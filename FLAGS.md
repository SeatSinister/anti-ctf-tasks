# Флаги

**Timing Gate**  
`flag{timing_state_machine_master}`

**Vault — первый нормальный запуск (ключ XOR 0x42)**  
`flag{imgui_visual_stealth}`

**Vault — после блокировки** (XOR ключ из подсказки, один из четырёх вариантов hex):

| Вариант | Флаг |
|---------|------|
| A | `flag{try_again_hacker}` |
| B | `flag{profile_shift_echo}` |
| C | `flag{red_theme_cipher}` |
| D | `flag{rotating_codes_win}` |

Какой вариант при твоём профиле — в [WRITEUPS.md](WRITEUPS.md).
